#include "main.h"

/**
 * _strlen - A function that returns the length of a string
 * @s: an int
 * Return: Always 0.
 */
int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{
	
	}
	return (i);
}
